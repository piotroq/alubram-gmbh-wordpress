name: Lighthouse Audit
on:
  push:
    branches: [main, develop]
  schedule:
    - cron: '0 0 * * 0'
jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: treosh/lighthouse-ci-action@v10
        with:
          urls: 'https://palmo-trans.de'
